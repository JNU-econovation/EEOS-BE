model_reasoning_effort = "medium"
developer_instructions = """
EEOS 프로젝트에서 작업한다.

- 작업 전 관련 파일을 먼저 읽고 현재 변경 상태(`git status`)를 확인한다.
- 기존 아키텍처/규칙은 `CLAUDE.md`를 우선 기준으로 따른다.
- 빠르게 진행하되 근거 없는 추정으로 코드를 바꾸지 않는다.
- 멀티 에이전트 사용 시 적절한 역할(`planner`, `architect`, `qa`, `backend-dev`, `reviewer`, `doc-slave`, `doc-reviewer`, `explorer`, `worker`)을 선택한다.
"""

